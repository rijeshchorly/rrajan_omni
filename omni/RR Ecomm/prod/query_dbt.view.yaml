# Reference this view as prod__query_dbt
schema: prod
table_name: query_dbt

dimensions:
  dummy: {}

measures:
  count:
    aggregate_type: count

#The info below was pulled from your dbt repository and is read-only.
dbt:
  name: query_dbt
  target_schema: prod
  config:
    materialized: view
  code: SELECT TRUE AS "dummy"
